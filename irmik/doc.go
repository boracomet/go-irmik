// Package irmik is a Gin meta-framework for server-rendered pages, sessions,
// and an opt-in catalog (auth, admin, upload, queues, images, and more).
//
// In development, [New] injects a small overlay (badge, errors, live reload via
// irmik dev). Those routes are not mounted in production.
package irmik
